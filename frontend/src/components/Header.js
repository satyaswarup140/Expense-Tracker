import React from 'react';
import './Header.css';  // Correct path to the CSS file

const Header = () => {
  return (
    <div className="header">
      <h1>Personal Expense Tracker</h1>
    </div>
  );
};

export default Header;
